# first install pandas using pip install pandas, coz pandas is a external module

import pandas
import hashlib # this module is build-in module, so no need to install it in terminal
import tensorflow # this module is a external module, so need to install using pip install tensorflow

print("hi")





